name: Deploy frontend to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - "guildsystem-frontend/**"
      - "src/**"
      - "public/**"
      - "angular.json"
      - "package.json"
      - "package-lock.json"
      - "tsconfig.json"
      - "tsconfig.app.json"
      - ".github/workflows/deploy-github-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Resolve frontend directory (monorepo subfolder vs repo root)
        id: fe
        run: |
          if [ -f guildsystem-frontend/package-lock.json ]; then
            echo "dir=guildsystem-frontend" >> $GITHUB_OUTPUT
          elif [ -f package-lock.json ]; then
            echo "dir=." >> $GITHUB_OUTPUT
          else
            echo "::error::package-lock.json not found at ./ or guildsystem-frontend/"
            exit 1
          fi

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: ${{ steps.fe.outputs.dir }}/package-lock.json

      - name: Install dependencies
        working-directory: ${{ steps.fe.outputs.dir }}
        run: npm ci

      - name: Build
        working-directory: ${{ steps.fe.outputs.dir }}
        env:
          PAGES_BASE_HREF_VAR: ${{ vars.PAGES_BASE_HREF }}
        run: |
          if [ -n "$PAGES_BASE_HREF_VAR" ]; then
            BASE_HREF="$PAGES_BASE_HREF_VAR"
          else
            BASE_HREF="/${{ github.event.repository.name }}/"
          fi
          npm run build -- --base-href "$BASE_HREF"

      - name: SPA fallback (copy index.html to 404.html)
        run: cp ${{ steps.fe.outputs.dir }}/dist/frontend/browser/index.html ${{ steps.fe.outputs.dir }}/dist/frontend/browser/404.html

      - uses: actions/upload-pages-artifact@v3
        with:
          path: ${{ steps.fe.outputs.dir }}/dist/frontend/browser

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
